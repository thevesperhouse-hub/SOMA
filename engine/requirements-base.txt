# Serveur + mode démo (aucune dep GPU). Suffit pour lancer le dashboard live.
# Versions à jour (juin 2026).
fastapi==0.138.2
uvicorn[standard]==0.49.0
pydantic>=2.11,<3
# TUI stylisé (launcher CLI : logo, panneaux, dashboard live)
rich>=13,<16
